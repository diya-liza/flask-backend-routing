Use following commands to virtual environment and activating it:
python -m venv env
gitbash: source env/Scripts/activate
cmd or powershell: ./env/Scripts/activate.bat

Use the following command to install dependcies from requirements.txt
pip install -r requirements.txt